h1 Add a new job
br
br
form.form(name='form', ng-submit='addJob(form)')
    .form-group
        .row
            label Input data (range as JSON array, e.g. [4,14])
        .row
            textarea.job-data(name='data', ng-model='data')
    .row
        button.btn.btn-lg.btn-primary(type='submit') Add job
        .div
        p.label.label-danger(ng-show='errors')
            | {{errors.message}}
        span.clearfix

